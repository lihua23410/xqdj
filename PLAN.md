# 后续模式（第一版不做）

第一版只做 1v1：选人页左右各一槽，开始、暂停、结束。下面两条写进计划，等 1v1 跑稳再接。

## 混战（2～6）

- 选人页改成 N 个槽，N 可调，允许同一角色多份。
- 开赛时在六边形内随机、不重叠投放（扫掠约束：圆心间距 ≥ 两半径之和 + skin）。
- 仍是自由出伤，单位各自 goroutine，无友军。
- 胜负：场上 `role=fighter` 只剩 1（或 0 为平局）。
- WebSocket `start` 增加 `slots: string[]`。

## 两队对战

- 槽位分成两边（2v2 / 3v3），同角色仍可重复。
- 单位带 `team`。感知包可含阵营；是否打友军由该角色自己的方法决定，引擎不写友军规则。
- 胜负：某一队 fighter 清零。
- 投放：按边分区出生，避免开局立刻贴脸。
