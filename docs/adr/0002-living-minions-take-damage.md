# 活随从走伤害管道，不计入胜负

教父的随从有生命、能被打掉，但现有伤害管道只认战斗机。脚本私有计数会让激光、墙刮、子弹各写一套例外，和「打战斗机的也打活随从」对不上。

决定：`Spec.Mortal` 的单位吃 `Damage` / `Heal`，生命归零则消失，不走确认伤害、不停帧、不换位、不计入胜负。出伤用 `Hittable`。索敌不再只认战斗机，见 ADR 0006。

不采用「所有随从 MaxHP 都可打」：子弹和弧的 MaxHP=1 只是占位，打它们会拆掉现有弹道。
