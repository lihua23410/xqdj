# 索敌看可瞄准，不看战斗机

活随从进场后，索敌仍写死敌方战斗机就瞄不到它们。不做仇恨表：资格是可瞄准，排序是瞄准优先度（越小越先瞄）。未写时战斗机 15、活随从 60、其余 0 且不能改成可瞄准；出伤仍用 `Hittable`（ADR 0002 的伤害管道不动）。1–255 时谁都能改，后写覆盖；改成 0 后只有自己能改回来。凡是挑一个瞄准对象都走这套；每次取数字最小、相同则当下最近，不另做全局锁。
